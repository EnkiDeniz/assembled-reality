# Loegos Git History Summary

**Export date:** April 7, 2026  
**Raw export:** `docs/LoegosSeed/git-history/full-commit-history-2026-04-07.txt`

This file is the readable history witness for the seed corpus. It summarizes the major arcs visible in the repository’s git history as of the export date above.

## Arc 1: Initial scaffold and document viewer

The project starts as a document-viewing shell and basic web app scaffold.

Representative commits:

- `c71b1d6` Initialize package.json with project details
- `e21b4d0` Initial commit: Assembled Reality document viewer
- `7d7e910` Add collaborative reading features + mobile-first responsive design

## Arc 2: Reader, library, and listening runtime

The repo then moves into a reader-first product: import, library, listening, and authenticated reading surfaces.

Representative commits:

- `f05f877` Rebuild reader listen experience
- `c4bbe7d` Stabilize reader listen state sync
- `ef8a4c2` Refine auth-first library import flow
- `781c975` Build prototype-faithful workspace

## Arc 3: Document assembler and source-intake expansion

The product broadens from reading into assembly: paste, link import, voice capture, and AI-assisted staging become first-class.

Representative commits:

- `610e2b5` Add anything-to-blocks intake system
- `d4b19ad` Add paste-first intake flow
- `e9825fa` Improve source cleanup and AI staging UX
- `abb88ed` Upgrade home loop for speak and drop-anything

## Arc 4: Boxes, Operate, and the Loegos shift

The repo then converges on the box model. Operate, box home, boxes index, and the Think/Create/Operate framing all land in this stretch.

Representative commits:

- `954dd1d` Add Operate box read engine
- `1de6c08` Migrate workspace language to boxes
- `328acb2` Add dedicated boxes index view
- `debd075` Align shell to think create operate loop
- `049a672` Reframe workspace as Box IDE shell

## Arc 5: Proof, receipts, and the self-assembly corpus

The repo begins preserving its own origin as source material. The self-assembly demo, receipt witnesses, and origin corpus are added directly into the product and docs.

Representative commits:

- `0896be4` Add founding session receipt to origin corpus
- `77a88f3` Seed the Loegos origin example box
- `835dcae` Add self-assembly demo box
- `96cbd6f` Add pivot source documents for self-assembly

## Arc 6: Founder flow, inline Operate, and proof closure

The most recent stretch builds the source-first founder path, inline Operate overlay, attested overrides, sealing guardrails, and the Founder Shell renderer.

Representative commits:

- `251e645` Implement phase 1 inline operate overlay
- `9396cb7` Harden inline operate trust rules
- `6a798be` Extract workspace controllers and add E2E harness
- `9fd03e7` Close phase 1 proof loop
- `f9dfc0f` Build Founder Shell source-first front door
- `1ee6344` Implement founder shell Loegos renderer v0.1
- `dea9ae6` Profile local proof workspace load

## Reading Note

This summary is intentionally compressed. For the full file-by-file history witness, use the raw export alongside this summary.
